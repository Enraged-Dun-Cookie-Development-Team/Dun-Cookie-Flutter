import 'package:flutter/material.dart';

class ToolAnnouncementBeta extends StatelessWidget {
  const ToolAnnouncementBeta({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
